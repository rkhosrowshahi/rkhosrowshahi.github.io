<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://rkhosrowshahi.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://rkhosrowshahi.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-13T21:01:26+00:00</updated><id>https://rkhosrowshahi.github.io/feed.xml</id><title type="html">blank</title><subtitle>PhD student researching AI/ML, gradient-free neural network training, memory reduction, and large-scale optimization. </subtitle><entry><title type="html">CurveBench: how neural networks learn curves</title><link href="https://rkhosrowshahi.github.io/blog/2026/curvebench/" rel="alternate" type="text/html" title="CurveBench: how neural networks learn curves"/><published>2026-07-13T00:00:00+00:00</published><updated>2026-07-13T00:00:00+00:00</updated><id>https://rkhosrowshahi.github.io/blog/2026/curvebench</id><content type="html" xml:base="https://rkhosrowshahi.github.io/blog/2026/curvebench/"><![CDATA[<p>How does a neural network learn a curve?</p> <p>Not from a single loss number at the end, but from the shape of the prediction as training progresses. Does the fitted curve smooth out harmonic by harmonic? Does a wide net lock in quickly while a narrow one lags? Does SGD diverge while Adam stabilizes? Does a population-based search slowly crawl toward the target?</p> <p><a href="https://github.com/rkhosrowshahi/curvebench">CurveBench</a> is a small benchmark built to watch that process. Three ReLU MLPs (narrow, medium, and wide) learn the same 1D target side by side. Each run exports a training video: orange dots are the target, colored lines are the evolving prediction.</p> <hr/> <h2 id="the-target">The target</h2> <p>We start with a smooth Fourier sum on $x \in [-10, 10]$:</p> \[y = \sin(x) + \tfrac{1}{2}\sin(2x) + \tfrac{1}{3}\sin(3x)\] <p>Three networks, same depth, different width:</p> <table> <thead> <tr> <th>Network</th> <th>$d$</th> <th>Parameters</th> </tr> </thead> <tbody> <tr> <td>N1</td> <td><code class="language-plaintext highlighter-rouge">[1, 10, 10, 1]</code></td> <td>141</td> </tr> <tr> <td>N2</td> <td><code class="language-plaintext highlighter-rouge">[1, 100, 100, 1]</code></td> <td>10,401</td> </tr> <tr> <td>N3</td> <td><code class="language-plaintext highlighter-rouge">[1, 1000, 1000, 1]</code></td> <td>1,004,001</td> </tr> </tbody> </table> <p>A ReLU net builds its prediction from piecewise linear segments. A wider hidden layer can allocate more segments, so in principle a wide net has more flexibility to trace a smooth oscillating curve, but only if the optimizer actually finds a good weight configuration.</p> <p>Gradient-descent runs use 2000 steps (GFLOP-matched budget per network), each under constant learning rate or cosine annealing to $\eta_{\min} = 0$. Evolutionary runs use the same GFLOP budget with a 6000 function-evaluation cap (population size 100). Seed 123 throughout.</p> <hr/> <h2 id="gradient-descent">Gradient descent</h2> <p>Every GD optimizer below is run twice: fixed step size for all 2000 steps (const), or cosine decay from the initial rate down to zero (cosine).</p> <h3 id="learning-rate-schedule-matters">Learning-rate schedule matters</h3> <p>On this problem the schedule interacts strongly with width. Constant LR tends to win on the wide nets (N2, N3) for Adam, AdamW, and RMSprop, because the step size stays large enough to keep fitting late in training. Cosine annealing helps on N1 for L-BFGS and RMSprop, where a long fine-tuning tail polishes the narrow net. SGD under either schedule lags the adaptive methods.</p> <table> <thead> <tr> <th>Optimizer</th> <th>Schedule</th> <th>N1 MSE</th> <th>N2 MSE</th> <th>N3 MSE</th> </tr> </thead> <tbody> <tr> <td>RMSprop</td> <td>const</td> <td>0.017</td> <td><strong>0.004</strong></td> <td><strong>0.003</strong></td> </tr> <tr> <td>RMSprop</td> <td>cosine</td> <td><strong>0.012</strong></td> <td>0.006</td> <td>0.004</td> </tr> <tr> <td>Adam</td> <td>const</td> <td><strong>0.173</strong></td> <td><strong>0.009</strong></td> <td><strong>0.013</strong></td> </tr> <tr> <td>Adam</td> <td>cosine</td> <td>0.246</td> <td>0.011</td> <td>0.073</td> </tr> <tr> <td>AdamW</td> <td>const</td> <td><strong>0.173</strong></td> <td><strong>0.009</strong></td> <td><strong>0.011</strong></td> </tr> <tr> <td>AdamW</td> <td>cosine</td> <td>0.246</td> <td>0.011</td> <td>0.077</td> </tr> <tr> <td>SGD</td> <td>const</td> <td><strong>0.506</strong></td> <td><strong>0.290</strong></td> <td><strong>0.063</strong></td> </tr> <tr> <td>SGD</td> <td>cosine</td> <td>0.595</td> <td>0.324</td> <td>0.186</td> </tr> <tr> <td>L-BFGS</td> <td>const</td> <td>0.666</td> <td>0.571</td> <td>0.478</td> </tr> <tr> <td>L-BFGS</td> <td>cosine</td> <td><strong>0.232</strong></td> <td><strong>0.307</strong></td> <td><strong>0.315</strong></td> </tr> </tbody> </table> <p>Bold = better of const vs cosine for that optimizer and width.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-adam-const-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> Adam, constant LR, 2000 steps (N1 / N2 / N3 stacked). N3 tracks the target well. Cosine annealing on the same budget is worse on the wide nets. </div> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-adam-cosine-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> Adam, cosine LR, 2000 steps. The learning rate decays too quickly for N2 and N3 to reach the same fit. </div> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-rmsprop-const-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> RMSprop, constant LR. Best overall N2 and N3 MSE in the GFLOP-matched GD sweep. </div> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-rmsprop-cosine-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> RMSprop, cosine LR. Slightly better on N1, slightly worse on N2 and N3 than const. </div> <p>AdamW mirrors Adam under both schedules. SGD trails the adaptive methods, and const LR is better on every width. L-BFGS struggles with const LR, but cosine annealing improves the narrow net. On N2 and N3 it still lags RMSprop and Adam.</p> <hr/> <h2 id="evolutionary-algorithms-at-matched-compute">Evolutionary algorithms at matched compute</h2> <p>Population-based methods do not use gradients. They propose weight vectors, score them by MSE, and iterate. We compared five EAs under the same GFLOP budget as 2000 GD steps (~6000 forward passes per network, population 100):</p> <table> <thead> <tr> <th>Method</th> <th>N1 MSE</th> <th>N2 MSE</th> <th>N3 MSE</th> </tr> </thead> <tbody> <tr> <td>DE (F=0.5, elitist)</td> <td>0.606</td> <td><strong>0.416</strong></td> <td>0.515</td> </tr> <tr> <td>jDE (adaptive F, CR, gauss init)</td> <td>0.485</td> <td>0.532</td> <td><strong>0.465</strong></td> </tr> <tr> <td>PSO (w=0.9, c1=c2=2)</td> <td>0.479</td> <td>0.599</td> <td>0.625</td> </tr> <tr> <td>PSO (w=0.5)</td> <td><strong>0.448</strong></td> <td>0.550</td> <td>0.557</td> </tr> <tr> <td>Sep-CMA-ES</td> <td>0.478</td> <td>0.563</td> <td>1.555</td> </tr> </tbody> </table> <p>No single EA wins on every width. DE is strongest on the medium net (N2). jDE adapts mutation and crossover rates per individual and takes N3. PSO with lower inertia ($w = 0.5$) beats $w = 0.9$ everywhere on this task, with less momentum and more responsiveness to personal and global bests. Sep-CMA-ES keeps up on small search spaces but collapses on the million-parameter net.</p> <p>Each clip stacks N1 (top), N2 (middle), and N3 (bottom) so you can watch all three widths learn under the same algorithm.</p> <h3 id="differential-evolution">Differential Evolution</h3> <p>DE proposes trial vectors by mixing population members and accepts improvements. On N2 the prediction visibly tightens around the harmonics. On N3 progress is slower but steady.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-de-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> DE ($F = 0.5$), GFLOP-fair budget. Best N2 MSE among EAs. N1 prediction improves but stays rougher than gradient methods. </div> <h3 id="jde">jDE</h3> <p>jDE carries its own mutation scale $F$ and crossover rate CR for each individual, resampling them occasionally and keeping successful pairs. Gaussian (Kaiming) initialization gives each swarm member a distinct starting function, which matters for diversity on high-dimensional weight vectors.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-jde-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> jDE, GFLOP-fair budget. Best N3 MSE. Watch the bottom panel (purple) track the target while Sep-CMA-ES on N3 fails. </div> <h3 id="particle-swarm-optimization">Particle Swarm Optimization</h3> <p>We implemented pymoo-style PSO with fixed hyperparameters: per-dimension random coefficients and velocity clipping. Lower inertia ($w = 0.5$) consistently outperforms $w = 0.9$ here.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-pso-w05-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> PSO ($w = 0.5$), GFLOP-fair budget. Best N1 MSE among EAs. Particles drift toward the global best without backprop. </div> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-pso-w09-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> PSO ($w = 0.9$), same budget. Higher inertia means slower reaction and visibly worse fits on N2 and N3. </div> <h3 id="sep-cma-es">Sep-CMA-ES</h3> <p>Diagonal CMA-ES adapts a per-weight step size. We initialize the search with $\sigma = 0.1$ as the initial standard deviation (<code class="language-plaintext highlighter-rouge">std_init</code>) of the Gaussian sampling distribution. It is competitive on N1 and N2, but the N3 panel barely moves because the search space is too large for this population budget.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-sep-cmaes-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> Sep-CMA-ES ($\sigma = 0.1$), GFLOP-fair budget. Steady on N1/N2. N3 (bottom) stagnates. </div> <hr/> <h2 id="small-efficient-neural-networks-with-infinite-compute">Small efficient neural networks with infinite compute</h2> <p>N1 has only 141 parameters, yet it is the hardest net to fit well under a tight EA budget and the easiest to polish when function evaluations are cheap. Raising the FE cap changes which algorithm wins and exposes how much population initialization and popsize matter.</p> <h3 id="population-init-center_gaussian">Population init: <code class="language-plaintext highlighter-rouge">center_gaussian</code></h3> <p>We added three init modes. <code class="language-plaintext highlighter-rouge">center_gaussian</code> matches the original CurveBench recipe: one PyTorch seed network, a cloud of perturbations $\text{center} + \sigma \cdot \mathcal{N}(0, 1)$, and the exact center as one swarm member. <code class="language-plaintext highlighter-rouge">gaussian</code> gives every member an independent Kaiming draw plus a zero vector. <code class="language-plaintext highlighter-rouge">uniform</code> samples $U(-\sigma, \sigma)$ plus a zero vector.</p> <p>On N1 at 1M FEs, DE with <code class="language-plaintext highlighter-rouge">center_gaussian</code> and popsize $\lceil 10\sqrt{D}\rceil = 119$ reaches MSE 0.0066. The same run with popsize 100 stalls near 0.17. Independent Kaiming init (<code class="language-plaintext highlighter-rouge">gaussian</code>) behaves differently again. Init is not a detail here. It sets whether the search starts near one reasonable function or spreads across incompatible ones.</p> <h3 id="differential-evolution-at-raised-budgets">Differential Evolution at raised budgets</h3> <p>DE uses $F = 0.5$, elitism, and <code class="language-plaintext highlighter-rouge">center_gaussian</code> with $\sigma = 0.1$. N1 and N2 use popsize $\lceil 10\sqrt{D}\rceil$. N3 is capped at 10k FEs and popsize 20 because a million-parameter forward pass per individual is too expensive at full budget.</p> <table> <thead> <tr> <th>Network</th> <th>FEs</th> <th>Pop</th> <th>Final MSE</th> </tr> </thead> <tbody> <tr> <td>N1</td> <td>1,000,000</td> <td>119</td> <td>0.0066</td> </tr> <tr> <td>N2</td> <td>1,000,000</td> <td>1,020</td> <td>0.188</td> </tr> <tr> <td>N3</td> <td>10,000</td> <td>20</td> <td>0.401</td> </tr> </tbody> </table> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/curvebench/fourier3-de-unlimited-composed.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> DE with $F = 0.5$ at raised FE budgets (N1 / N2 / N3 stacked). N1 fits tightly. N2 improves but lags gradient methods. N3 moves slightly within 10k FEs on a million-parameter net. </div> <p>N1 under loose compute beats every GFLOP-matched entry in the summary tables below, including RMSprop at 0.012. Patience and a well-centered cloud can trace harmonics that short-budget gradient steps leave rough on the same architecture.</p> <h3 id="pso-catches-up-when-init-and-velocity-match-the-problem">PSO catches up when init and velocity match the problem</h3> <p>The GFLOP-fair PSO sweep used independent Kaiming init (<code class="language-plaintext highlighter-rouge">gaussian</code>) and random initial velocities. Lower inertia ($w = 0.5$) won N1 at 0.448. Re-running PSO on N1 alone with <code class="language-plaintext highlighter-rouge">center_gaussian</code> at 1M FEs tells a different story.</p> <p>We fixed $c_1 = c_2 = 2$, popsize 119, zero initial velocity, and swept $w$:</p> <table> <thead> <tr> <th>Inertia $w$</th> <th>Init velocity</th> <th>1M FEs, N1 MSE</th> </tr> </thead> <tbody> <tr> <td>0.9</td> <td>random</td> <td>0.353</td> </tr> <tr> <td>0.9</td> <td>zero</td> <td>0.339</td> </tr> <tr> <td>0.5</td> <td>random</td> <td>0.145</td> </tr> <tr> <td>0.5</td> <td>zero</td> <td>0.0095</td> </tr> <tr> <td>0.25</td> <td>zero</td> <td>0.012</td> </tr> <tr> <td>0.1</td> <td>zero</td> <td>0.016</td> </tr> </tbody> </table> <p>Random velocity adds noise that helps at $w = 0.9$ but hurts at $w = 0.5$. Zero velocity lets low-inertia particles converge toward personal and global bests without overshooting. At $w = 0.5$ with zero velocity, PSO reaches 0.0095, within striking distance of DE (0.0066) on the same net and budget.</p> <p>Two practical lessons from the N1 unlimited-compute sweep:</p> <ol> <li>Match popsize to dimension ($\approx 10\sqrt{D}$ for DE on small nets).</li> <li>Match PSO inertia to initialization: <code class="language-plaintext highlighter-rouge">center_gaussian</code> pairs with low $w$ and zero velocity. High $w$ with a tight cloud around one seed barely moves.</li> </ol> <p>Wide nets (N2, N3) still favor gradient methods even after raising the EA budget, but the smallest net is where evolutionary search closes the gap if you give it enough evaluations and the right starting geometry.</p> <hr/> <h2 id="a-different-question-learning-sinx-beyond-the-training-interval">A different question: learning $\sin(x)$ beyond the training interval</h2> <p>The Fourier experiment trains and tests on the same interval. A second experiment changes the question: can the network extrapolate?</p> <p>Target: $y = \sin(x)$. Train on $x \in [-1, 1]$, evaluate on $x \in [-3, 3]$.</p> <p>With ReLU activations, the network learns reasonable fits inside the training band, but outside it the prediction is piecewise-linear. It cannot rediscover periodicity it never saw. Swap the activation to $\sin$, and the same architecture can extend the wave into the out-of-distribution regions, because the building blocks themselves are periodic.</p> <p>That experiment is less about optimizer choice and more about what function class the network can express before training even starts.</p> <hr/> <h2 id="summary-all-methods-gflop-matched-budget">Summary: all methods (GFLOP-matched budget)</h2> <p>Final train MSE on the Fourier target. Gradient methods: 2000 steps, reported separately for const and cosine LR. Evolutionary methods: same GFLOP budget, ~6000 function evaluations, population 100.</p> <h3 id="gradient-descent-1">Gradient descent</h3> <table> <thead> <tr> <th>Method</th> <th>N1 MSE</th> <th>N2 MSE</th> <th>N3 MSE</th> </tr> </thead> <tbody> <tr> <td>RMSprop (const)</td> <td>0.017</td> <td><strong>0.004</strong></td> <td><strong>0.003</strong></td> </tr> <tr> <td>RMSprop (cosine)</td> <td><strong>0.012</strong></td> <td>0.006</td> <td>0.004</td> </tr> <tr> <td>Adam (const)</td> <td><strong>0.173</strong></td> <td><strong>0.009</strong></td> <td><strong>0.013</strong></td> </tr> <tr> <td>Adam (cosine)</td> <td>0.246</td> <td>0.011</td> <td>0.073</td> </tr> <tr> <td>AdamW (const)</td> <td><strong>0.173</strong></td> <td><strong>0.009</strong></td> <td><strong>0.011</strong></td> </tr> <tr> <td>AdamW (cosine)</td> <td>0.246</td> <td>0.011</td> <td>0.077</td> </tr> <tr> <td>SGD (const)</td> <td><strong>0.506</strong></td> <td><strong>0.290</strong></td> <td><strong>0.063</strong></td> </tr> <tr> <td>SGD (cosine)</td> <td>0.595</td> <td>0.324</td> <td>0.186</td> </tr> <tr> <td>L-BFGS (const)</td> <td>0.666</td> <td>0.571</td> <td>0.478</td> </tr> <tr> <td>L-BFGS (cosine)</td> <td><strong>0.232</strong></td> <td><strong>0.307</strong></td> <td><strong>0.315</strong></td> </tr> </tbody> </table> <h3 id="evolutionary-algorithms">Evolutionary algorithms</h3> <table> <thead> <tr> <th>Method</th> <th>N1 MSE</th> <th>N2 MSE</th> <th>N3 MSE</th> </tr> </thead> <tbody> <tr> <td>DE (F=0.5, elitist)</td> <td>0.606</td> <td><strong>0.416</strong></td> <td>0.515</td> </tr> <tr> <td>jDE (adaptive F, CR, gauss init)</td> <td>0.485</td> <td>0.532</td> <td><strong>0.465</strong></td> </tr> <tr> <td>PSO (w=0.9, c1=c2=2)</td> <td>0.479</td> <td>0.599</td> <td>0.625</td> </tr> <tr> <td>PSO (w=0.5)</td> <td><strong>0.448</strong></td> <td>0.550</td> <td>0.557</td> </tr> <tr> <td>Sep-CMA-ES ($\sigma=0.1$)</td> <td>0.478</td> <td>0.563</td> <td>1.555</td> </tr> </tbody> </table> <p>Bold in each GD block marks the better const vs cosine result for that optimizer and width. Overall GFLOP-matched winners: RMSprop (const) on N2 and N3, RMSprop (cosine) on N1. Among EAs: PSO (w=0.5) on N1, DE on N2, jDE on N3.</p> <p>With raised FE budgets on N1, DE reaches MSE 0.0066 and PSO ($w = 0.5$, zero velocity, <code class="language-plaintext highlighter-rouge">center_gaussian</code>) reaches 0.0095, both well below every GFLOP-fair entry above. N2 and N3 improve under loose EA budgets but remain above gradient methods at matched compute.</p> <hr/> <h2 id="what-the-videos-show">What the videos show</h2> <p>Stepping back from the numbers:</p> <ol> <li>Width changes how many linear pieces are available, but does not guarantee a better learned curve under every optimizer.</li> <li>Optimizer changes the trajectory. Adam stabilizes wide nets with const LR. Cosine annealing hurts wide nets here. RMSprop excels overall. L-BFGS only becomes competitive on N1 with cosine decay. EAs crawl without gradients at matched compute, but on N1 with 1M FEs, DE and tuned PSO can approach gradient quality when init and popsize are right.</li> <li>Activation encodes prior knowledge about the shape of the world. That matters when the test domain extends beyond training.</li> </ol> <p>CurveBench exists to make those differences visible. The <a href="https://github.com/rkhosrowshahi/curvebench">code and configs</a> are open source (MIT) if you want to run your own targets and watch your own curves learn.</p>]]></content><author><name></name></author><category term="research"/><category term="research"/><category term="machine-learning"/><category term="optimization"/><category term="neural-networks"/><summary type="html"><![CDATA[Watching MLPs learn 1D curves. Width, optimizer, and activation change what the prediction looks like as training unfolds.]]></summary></entry><entry><title type="html">Gradient Descent’s Hidden Tax: Rethinking How We Compare It to Gradient-Free Methods</title><link href="https://rkhosrowshahi.github.io/blog/2026/gradient-descent-hidden-tax/" rel="alternate" type="text/html" title="Gradient Descent’s Hidden Tax: Rethinking How We Compare It to Gradient-Free Methods"/><published>2026-07-13T00:00:00+00:00</published><updated>2026-07-13T00:00:00+00:00</updated><id>https://rkhosrowshahi.github.io/blog/2026/gradient-descent-hidden-tax</id><content type="html" xml:base="https://rkhosrowshahi.github.io/blog/2026/gradient-descent-hidden-tax/"><![CDATA[<h2 id="tldr">TL;DR</h2> <p>Every gradient descent (GD) step in a neural network costs roughly three times the compute of a gradient-free (GF) step. GD needs one forward pass plus two backward passes per layer (except the input layer, which only needs a weight gradient, not an input gradient, since nothing upstream needs it). GF methods only need a forward pass: there’s no gradient to compute, so the backward graph never gets built on the GPU at all. If you’re benchmarking GD against GF optimizers, giving both the same number of steps or epochs is not a fair fight. Give the GF method roughly three times the steps for the same compute budget, or better, match on FLOPs rather than steps.</p> <hr/> <h2 id="where-the-2x-for-backward-comes-from">Where the 2x for backward comes from</h2> <p>Backpropagation at a single layer has to solve two separate problems, not one:</p> <p>Suppose layer $l$ takes in an input vector $x_{l-1}$ (the output of the previous layer), applies weights $W_l$ to it, and passes the result forward. $L$ is the scalar loss computed at the very end of the network, the single number the whole training run is trying to shrink. With that setup:</p> <ol> <li><strong>Gradient with respect to the weights</strong>, $\dfrac{\partial L}{\partial W_l}$, so the optimizer knows how to update that layer.</li> <li><strong>Gradient with respect to the layer’s input</strong>, $\dfrac{\partial L}{\partial x_{l-1}}$, so the error signal can keep flowing backward to earlier layers.</li> </ol> <p>Each of those is roughly the same amount of matrix multiplication as the forward pass itself. So a full backward pass through a layer costs about twice what the forward pass through that same layer cost. Add the forward pass back in, and one GD step is forward (1x) plus backward (2x) equals 3x the compute of a bare forward pass.</p> <p>The input layer is the one exception worth naming: since nothing feeds into it from an earlier layer, there’s no need to compute the input gradient there, only the weight gradient. In small networks this shaves off a noticeable sliver of compute; in deep networks with dozens or hundreds of layers, it rounds to a footnote.</p> <p>Putting numbers to it: for a layer $l$ with input dimension $d_{l-1}$ and output dimension $d_l$, the forward pass costs approximately</p> \[F_l \approx 2\, d_{l-1} d_l\] <p>FLOPs per example (the factor of 2 is the standard multiply-add convention). Backward through that same layer needs two gradients, each costing about as much as the forward pass:</p> \[\frac{\partial L}{\partial W_l} \approx 2\, d_{l-1} d_l, \qquad \frac{\partial L}{\partial x_{l-1}} \approx 2\, d_{l-1} d_l\] <p>so a full hidden layer costs $F_l$ forward plus $2F_l$ backward, or $3F_l$ total. Only the input layer ($l = 1$) drops the second term, costing $2F_1$ instead of $3F_1$, since there’s no $x_0$ upstream to send a gradient to. Summed over a network of $L$ layers:</p> \[C_{GD} = 3\sum_{l=2}^{L} F_l \;+\; 2F_1 \;\approx\; 3\sum_{l=1}^{L} F_l\] <p>for any network deep enough that one layer’s discount doesn’t move the total.</p> <p>This isn’t a hand wavy estimate. It matches the standard FLOPs accounting used in neural scaling law research, most notably <a href="https://arxiv.org/abs/2001.08361">Kaplan et al., 2020</a>: forward pass compute per token is approximately</p> \[C_{fwd} \approx 2N\] <p>FLOPs, where $N$ is the count of non-embedding parameters ($N \approx \sum_l d_{l-1} d_l$, which is exactly $\sum_l F_l$ up to the per-example constant). A full training step, forward and backward together, comes out to approximately</p> \[C_{GD} = C_{fwd} + C_{bwd} \approx 2N + 4N = 6N\] <p>meaning backward alone, $C_{bwd} \approx 4N$, is about twice forward. Three components, one forward and two backward equivalents, all the same order of magnitude.</p> <hr/> <h2 id="why-gradient-free-methods-dodge-all-of-it">Why gradient-free methods dodge all of it</h2> <p>Gradient-free methods (<a href="https://arxiv.org/abs/1703.03864">evolution strategies</a>, genetic algorithms, random search, simulated annealing, zeroth-order optimization, CMA-ES, and similar) score a network by running it forward and reading off a loss or reward. There is no chain rule to unwind, so:</p> <ul> <li>No computation graph needs to be retained between layers.</li> <li>No backward kernels get launched on the GPU.</li> <li>No activation memory needs to be kept around for a later backward pass, which is a separate, often underrated win: it frees up memory for larger batches or larger populations in the same VRAM footprint.</li> </ul> <p>In the same notation as above, a GF evaluation only pays the forward term:</p> \[C_{GF} = C_{fwd} \approx 2N\] <p>which puts the ratio between the two at</p> \[\frac{C_{GD}}{C_{GF}} = \frac{6N}{2N} = 3\] <p>exactly the multiplier in the TL;DR, derived rather than asserted. That last point about memory matters more than it first appears, and it’s easy to miss if you only count FLOPs.</p> <hr/> <h2 id="does-the-3x-hold-across-layer-types-and-architectures">Does the 3x hold across layer types and architectures?</h2> <p>The derivation above used a generic dense layer as the working example, since it’s the cleanest case. Once other layer types enter the picture, the ratio holds cleanly for some and drifts for others.</p> <h3 id="matmul-dominated-layers-convolutions-and-linear-projections">Matmul-dominated layers: convolutions and linear projections</h3> <p>Convolutional layers are structured matrix multiplies (via im2col or an equivalent formulation), so a conv layer with $C_{in}$ input channels, $C_{out}$ output channels, kernel size $k \times k$, and output spatial size $H \times W$ costs approximately</p> \[F_{conv} \approx 2\, k^2 C_{in} C_{out} H W\] <p>per example. Backward needs the same two gradients as a dense layer, a weight gradient and an input gradient, each costing about $F_{conv}$. The 3x rule carries over almost unchanged: convolutional networks (ResNets, U-Nets, and similar) follow the $3F_l$ per layer accounting about as cleanly as fully connected networks do. Transformer projection layers (QKV, output projection, MLP up and down projections) are literally dense layers, so the same accounting applies directly to them too.</p> <h3 id="the-extra-term-in-attention">The extra term in attention</h3> <p>Attention adds a second cost source beyond the projections: the $QK^\top$ score matrix and the score-weighted sum over values, which scale with sequence length $L$ rather than with parameter count:</p> \[C_{attn} \approx 2 L^2 d_{model}\] <p>per layer. This term isn’t linear in $N$, so it sits outside the clean $6N$ approximation entirely. When $L \ll d_{model}$, it’s small next to the projection cost and the 3x figure holds well. At long context lengths it can dominate, and forward:backward for this term still tracks close to 1:2 for the same chain rule reason as everywhere else, so the ratio survives even in the regime where the flat $6N$ absolute figure stops being accurate.</p> <h3 id="where-the-ratio-drifts-below-2x-elementwise-layers">Where the ratio drifts below 2x: elementwise layers</h3> <p>Activations (ReLU, GELU, SiLU), dropout, and residual adds don’t have a weight gradient and input gradient to split apart, there’s just one gradient to compute: the upstream signal multiplied by the local derivative at that point. So a bare elementwise layer costs roughly one unit of compute forward and about one unit backward, not two, since there’s no weight to update. These layers are cheap enough in absolute FLOPs that they don’t move the network wide ratio much, but they’re a genuine exception to “every layer is 3x.”</p> <h3 id="where-the-ratio-drifts-above-2x-normalization-layers">Where the ratio drifts above 2x: normalization layers</h3> <p>LayerNorm and BatchNorm are deceptively expensive on the backward side. Forward computes a mean and variance and normalizes with them. Backward needs gradients with respect to the scale and shift parameters, plus a gradient with respect to the input that depends on the full mean and variance gradient contribution across the batch or feature group, a cross-term that forward never had to compute at all. In absolute FLOPs these layers stay small next to matmuls, but their local forward:backward ratio isn’t the clean 1:2 that dense layers get.</p> <h3 id="embedding-layers-asymmetric-by-construction">Embedding layers: asymmetric by construction</h3> <p>Forward is a lookup (a gather), essentially free. Backward is a scatter-add into the embedding table’s gradient, one update per token rather than a dense matmul. Cheap in both directions, but the assumption that forward and backward share the same underlying operation doesn’t really apply here, since neither direction is a matmul to begin with.</p> <h3 id="recurrent-layers-same-ratio-different-memory-story">Recurrent layers: same ratio, different memory story</h3> <p>RNNs and LSTMs run backpropagation through time, which unrolls the same $3F_l$ per step accounting across every timestep in the sequence. The compute ratio per step doesn’t change: forward is still one matmul, backward is still two. But every intermediate hidden state across the full sequence needs to survive until backward reaches it, which is the same kind of memory pressure that made activation checkpointing necessary for transformers, just applied per timestep instead of per layer.</p> <h3 id="mixture-of-experts-the-ratio-holds-the-base-count-shrinks">Mixture-of-Experts: the ratio holds, the base count shrinks</h3> <p>MoE layers route each token to a handful of active experts rather than running every expert densely. Forward only pays for the active experts plus the router. Backward routes gradients back through exactly those same active experts, so the one forward, two backward accounting still applies, just to a sparse subset of the network’s total parameters rather than all of them. A GF evaluation of the same MoE would also only pay for the active experts, since gradient-free evaluation is still nothing but a forward pass, so the 3x comparison holds at the reduced, sparse FLOP count rather than the dense one.</p> <h3 id="bringing-it-back-to-the-3x-rule">Bringing it back to the 3x rule</h3> <p>None of this breaks the headline result. The 3x still holds for the layers that dominate total network compute (dense projections and convolutions), and it’s a reasonable estimate for attention and MoE too once you account for what’s actually active. It just isn’t literally true layer by layer: elementwise layers pull the ratio down, normalization layers push it up, and recurrent or checkpointed architectures add memory costs that a flat FLOP ratio never captures in the first place. The practical implication from the earlier profiling proposal still stands: measure the real ratio for your own architecture rather than assuming the idealized 3x applies uniformly across every layer type in the network.</p> <hr/> <h2 id="the-benchmarking-mistake-this-causes">The benchmarking mistake this causes</h2> <p>A common setup in papers comparing GD to GF optimizers on neural networks: train network A with SGD/Adam for N epochs, train network B with an evolutionary strategy for N epochs or N generations, then compare final accuracy. This looks fair on paper because the step count matches. It isn’t fair, because each GD step did roughly 3x the work of each GF step. The GF method was handed a third of the compute budget and then judged against it.</p> <p>Making the correction explicit: at equal batch size, iso-compute between $S_{GD}$ gradient descent steps and $S_{GF}$ gradient-free steps requires</p> \[S_{GD} \cdot C_{GD} = S_{GF} \cdot C_{GF} \;\;\Longrightarrow\;\; S_{GF} = S_{GD} \cdot \frac{C_{GD}}{C_{GF}} = 3\, S_{GD}\] <p>so a fair comparison isn’t “run both for $N$ steps,” it’s “run GD for $N$ steps and GF for $3N$ steps,” or equivalently, plot both against $C_{GD} \cdot S_{GD}$ total FLOPs and read off whichever step count each method reaches at the same point on that axis.</p> <hr/> <h2 id="a-few-ideas-for-fixing-the-comparison">A few ideas for fixing the comparison</h2> <h3 id="1-report-learning-curves-against-flops-not-epochs">1. Report learning curves against FLOPs, not epochs</h3> <p>Plotting loss or accuracy against wall clock time or total FLOPs consumed, rather than against epoch count, removes the ambiguity entirely. An epoch axis hides the fact that the units on it aren’t the same size for both methods. A compute axis doesn’t.</p> <h3 id="2-dont-trust-a-flat-3x-measure-your-own-ratio">2. Don’t trust a flat 3x, measure your own ratio</h3> <p>The 3x figure is a clean idealization. Real ratios drift depending on mixed precision, activation checkpointing (which trades extra recomputation for less memory, changing the backward cost), fused kernels, and optimizer state overhead (Adam keeps two extra moment buffers per parameter, adding update cost that a GF method never pays either). Before adjusting step counts, it’s worth profiling the actual forward to backward wall clock ratio for your own architecture and hardware, using something like <a href="https://docs.pytorch.org/docs/stable/profiler.html"><code class="language-plaintext highlighter-rouge">torch.profiler</code></a> or a FLOP counter such as <a href="https://github.com/facebookresearch/fvcore"><code class="language-plaintext highlighter-rouge">fvcore</code></a>, rather than assuming the textbook number holds exactly.</p> <h3 id="3-let-gf-spend-its-memory-savings-dont-just-give-it-more-steps">3. Let GF spend its memory savings, don’t just give it more steps</h3> <p>Since GF doesn’t hold onto activations for backward, it can often run a larger population per generation in the same memory as one GD forward-backward pass. A more honest comparison protocol matches both compute and memory: instead of a flat step multiplier, size the GF population so that peak memory usage across a generation equals the peak memory of one GD step, then compare at matched wall clock time. This tends to be more favorable to GF than a naive 3x step correction alone, since it captures a resource GD is quietly consuming that a step count comparison ignores.</p> <h3 id="4-try-a-two-phase-hybrid-schedule-as-a-benchmark-baseline">4. Try a two phase hybrid schedule as a benchmark baseline</h3> <p>Rather than treating GD and GF as competitors that must be scored on identical axes, a useful baseline is to spend an early compute budget on cheap GF exploration (many rough forward-only evaluations to locate a promising region of parameter space), then hand off to a smaller number of expensive GD steps for fine-grained convergence. Comparing this hybrid against pure GD and pure GF, all at matched total FLOPs, tends to be more informative than any single method’s learning curve in isolation, and it turns the compute asymmetry into something to exploit rather than a nuisance to correct for.</p> <h3 id="5-push-for-a-standard-compute-clock-in-optimizer-leaderboards">5. Push for a standard “compute clock” in optimizer leaderboards</h3> <p>Most optimizer benchmarks report best accuracy at a fixed epoch or step count. A compute-normalized alternative, where every method’s x-axis is total FLOPs (or measured GPU-seconds on a reference device) instead of steps, would make GD-versus-GF comparisons, and really any comparison between methods with different per-step costs, apples to apples by construction rather than by afterthought correction.</p> <hr/> <h2 id="the-takeaway">The takeaway</h2> <p>The 3x rule is a useful default, not a universal constant. It’s grounded in real accounting (one forward, two backward, per layer, minus the input layer’s input gradient), and it lines up with the FLOPs ratios used in neural scaling law literature. But mixed precision, checkpointing, optimizer state, and memory headroom all nudge the true ratio around. The one thing worth taking away regardless of the exact multiplier: if a GD and a GF method are being compared on step count or epoch count alone, that comparison is measuring something other than what it claims to measure. Compute, not steps, is the currency that makes the comparison mean something.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Kaplan, J., McCandlish, S., Henighan, T., Brown, T. B., Chess, B., Child, R., Gray, S., Radford, A., Wu, J., &amp; Amodei, D. (2020). <a href="https://arxiv.org/abs/2001.08361">Scaling Laws for Neural Language Models</a>. arXiv:2001.08361.</li> <li>Salimans, T., Ho, J., Chen, X., Sidor, S., &amp; Sutskever, I. (2017). <a href="https://arxiv.org/abs/1703.03864">Evolution Strategies as a Scalable Alternative to Reinforcement Learning</a>. arXiv:1703.03864.</li> <li>PyTorch. <a href="https://docs.pytorch.org/docs/stable/profiler.html"><code class="language-plaintext highlighter-rouge">torch.profiler</code></a> documentation.</li> <li>Facebook Research. <a href="https://github.com/facebookresearch/fvcore"><code class="language-plaintext highlighter-rouge">fvcore</code></a>: a lightweight library with FLOP counting utilities.</li> </ul>]]></content><author><name></name></author><category term="research"/><category term="research"/><category term="machine-learning"/><category term="optimization"/><category term="neural-networks"/><category term="benchmarking"/><summary type="html"><![CDATA[Every GD step costs roughly 3× a gradient-free forward pass, so matching step counts isn't a fair benchmark. Match on FLOPs instead.]]></summary></entry></feed>