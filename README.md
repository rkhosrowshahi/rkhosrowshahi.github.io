# Rasa Khosrowshahi Academic Website

This site is built with [al-folio](https://github.com/alshedivat/al-folio), a Jekyll theme for academic websites.

## Local Development

```bash
bundle install
bundle exec jekyll serve
```

The previous React-based version is preserved on the `v1` branch.
